# Workflow-native definition template

Copy this directory to `<family>/<trial>/`, implement `definition.py`, and register the permanent
trial identity through the governing workflow study before formal outcome-relevant execution.
