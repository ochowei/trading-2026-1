# Decision

Accepted. Use `Workflow Release Activation` as the canonical term and version-root
`ACTIVATION.json` as immutable positive evidence. Record the v008 migration at the current time
with basis `grandfathered-effective-release`; do not backdate it. Keep v009 under the v008
canonical-merge bootstrap rule, and require explicit activation from v010 onward.

Human authority: `ochowei@gmail.com`.
