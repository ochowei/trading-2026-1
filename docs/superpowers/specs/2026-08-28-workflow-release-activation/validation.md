# Validation

Focused tests cover `draft -> prepared -> active`, exact `RELEASE.json` digest binding, immutable
activation evidence, tamper detection, prepared-successor study blocking, formal-execution
rejection for prepared versions, grandfathered migration attestation, and legacy historical replay.

The existing workflow authoring, study qualification, terminal evidence, and research suites pass
alongside the new tests. Repository validation, Ruff checks, fast regression, generated-index sync,
and whitespace validation remain required after the tracked workflow artifacts and documentation
are synchronized.
