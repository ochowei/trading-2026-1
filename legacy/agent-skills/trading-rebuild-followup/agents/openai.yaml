interface:
  display_name: "Trading: Rebuild Followup"
  short_description: "Rebuild followup strategies across every experiment asset"
  default_prompt: "Use $trading-rebuild-followup to rebuild followup strategies for all assets."
